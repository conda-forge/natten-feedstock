conda_build_tool: rattler-build
github:
  branch_name: main
  tooling_branch_name: main
conda_build:
  error_overlinking: true
conda_forge_output_validation: true
github_actions:
  # Self-hosted runners are not capped at 6 h like GitHub's own
  timeout_minutes: 720
provider:
  linux_64: github_actions
